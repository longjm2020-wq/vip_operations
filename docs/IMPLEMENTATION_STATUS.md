# 实施与验证记录 · 2026-09-13

## 2026-09-14 独立本机开发环境

从 GitHub main `bc71e42` 创建 `codex/local-development-20260914`，并于 2026-09-15 变基到最新 `origin/main` 的 `4a18bd6`。本机使用 Node 24.19.0、pnpm 10.32.1、独立 PostgreSQL 16.14 集群和 Redis 8.10.1，连接均限制在 127.0.0.1。新建 `vip_erp_dev`，完成 Prisma 生成、001 至 005 SQL 迁移与种子；未连接生产数据库或导入生产数据。随机凭据保存在忽略文件中。

本次实际通过 lint、typecheck、31 项单元测试、22 项独立数据库/API 场景、6 项浏览器测试和生产 build。启动后的编译版 smoke 验证 API 健康检查与 Web；单条表单和在线表格两种录入方式保留。构建仍有前端包体积告警，不影响启动。

启动器新增本机路径配置与 HTTP 就绪检查，解决原电脑固定路径依赖。配置方法见 [LOCAL_DEVELOPMENT.md](LOCAL_DEVELOPMENT.md)。本次未运行 Docker 或生产部署。

## 在线表格与 Excel 录入增量

保留原有单条表单，增加八类主数据的在线表格、XLSX/CSV 导入、列匹配预览、多行粘贴、撤销重做、失败行重试；采购与入库明细可切换录入方式，库存调整与建议确认支持批量，用户/角色的资料字段支持表格修改，列表支持当前页导出。详细范围和限制见 SPREADSHEET_ENTRY.md。

本次本地验证：lint、typecheck、生产 build、31 项单元测试、22 项真实数据库/API 场景通过。六项浏览器流程覆盖原有单条建档/采购/入库、Excel 建档/在线修改/失败重试、采购和入库的表格切换及真实库存调整。浏览器流程使用独立数据库，不写生产演示数据。

下文为早期阶段历史记录，不代表当前 Railway 部署状态。

## 颜色与尺码映射增量

已交付映射管理、SKU 规格下拉框、引用保护、事务审计及删除幂等。花色 025 有效，044 作废；初始化 64 项颜色、10 项尺码。数据库迁移 002 已应用。

本次验证：lint、typecheck、build、7 项单元测试、19 项真实 PostgreSQL/API 集成场景、3 项浏览器流程全部通过，包含映射增删改与采购分次入库。GitHub 首次提交使用清理后的完整快照；真实本地配置与登录资料不进入版本控制。

## 已交付

已建立本地Git仓库与codex/phase-1分支，交付可运行的内部商品—采购—入库—库存闭环。后台已具备商品/SKU/供应商/仓库、库存和流水、采购建议/采购单/入库、用户角色与审计页面。具体范围和工程差异见README与DECISIONS。

### M0：工程与运行

Monorepo、Node/pnpm锁定、React+Ant Design6、NestJS API、PostgreSQL、Redis/BullMQ Worker均已在原生Windows环境运行。前后端生产构建通过；本机启动入口为START_LOCAL.cmd。Docker Compose、Nginx和CI配置已写入，但未在Docker或远程CI实际运行。

### M1–M5：核心业务

SQL migration建表与seed完成，Prisma结构内省23个模型。登录、服务端会话、CSRF、权限、主数据写入审计、库存调整、采购确认、正常分次入库及采购建议已实现。库存写路径共享PostgreSQL事务，幂等记录和来源唯一约束同时防重复；历史流水/审计在数据库层禁止修改。

采购建议保留原始建议与人工确认量；生产/本机默认无销售源，不生成假建议。测试fixture验证计算与转PO。次品/少货不猜业务结清规则，异常过账返回BUSINESS_RULE_PENDING。

## 实际运行的检查

| 检查 | 结果 | 主要证据 |
|---|---|---|
| pnpm lint | 通过 | ESLint扫描应用、脚本、测试 |
| pnpm typecheck | 通过 | 前后端严格TypeScript检查 |
| pnpm test:unit | 7个通过 | 公式、截零取整、缺数据/0区分、状态、数据源隔离 |
| pnpm test:integration | 18个场景通过 | 真实PostgreSQL和独立API进程 |
| pnpm test:e2e | 2个流程通过 | Chrome真实界面、真实API、独立数据库 |
| pnpm test:queue | 通过 | Worker首次受控失败，第二次成功，attemptsMade=2 |
| pnpm build | 通过 | 后端编译、Vite产物 |
| migration与seed | 通过 | 新库建表、重复migration/seed不破坏数据 |
| 本机编译版smoke | 通过 | 登录、空业务库、OpenAPI、退出 |

18个集成场景覆盖：未登录/越权；无VOP建档；重复编码；调整幂等；确认后在途；部分入库；同Receipt同键和换键重复过账；完成剩余入库；已过账只读；两个连接并发超收；审计失败回滚全部业务写入；异常次品阻断；多仓；建议160/人工150快照；伪造状态；错仓；数据库历史防改；流水重建余额；迁移重跑。

浏览器流程覆盖：登录→建供应商/仓库/品类/商品/SKU→建采购草稿→编辑数量→提交/确认→40+60两次入库→自动完成→库存100→两笔流水与审计。另一流程验证未知销量跳过建议与退出后返回登录页。

测试期间发现并修复：驱动时间参数导致会话过期偏移；数据库void锁返回类型；约束错误码映射；数字字段命名转换；退出时查询缓存通知；测试服务启动就绪竞态。失败后均重新执行对应检查，不将失败报告当成通过。

## 尚未验收/未纳入的内容

- Docker构建/Compose启动、远程CI、生产部署与备份恢复演练：本次未运行。
- VOP 应用与 JIT+JITX 权限包已于 2026-09-15 在开放平台核验为运行中/审核通过；商品目录同步及 Railway 固定出口 IP 已完成配置。获批列表包含 `CompassDataOspService.data` 魔方罗盘取数接口，代码已加入 RSA 二次认证、只读取数客户端和不会输出响应数据的手动探针。当前魔方罗盘账号页面仍未显示公钥登记入口，也未提供具体指标 API code/参数，因此罗盘取数保持未启用；已提交唯品会支持服务单 `BZ2026091517000042`，待平台补齐这两项后，先执行不写生产库的最小取数验证。
- 真实销售/退货、矩阵分析、履约、财务、采购计划：后续阶段。
- 次品少货过账、部分取消/冲销、MOQ/配码、完整风险阈值：保留B01–B06对应门槛。
- 当前是首版业务工作台，部分高级筛选、图片管理和页面细节待后续完善；无虚假按钮模拟真实平台能力。
- 全局数据库写锁尚未做吞吐压力测试；Vite提示入口包约1.2MB，首版未做按页拆包优化。

因此，本次可交付“本机可用的核心MVP”，不宣称生产部署或全部后续规格已经完成。

## 数据分离

用户试用库：vip_erp_app，交接时商品、SKU、供应商、仓库均为空。测试库以vip_erp_test_/vip_erp_e2e_命名；早期联调库保留未删除。任何演示数字仅在测试库中，未混入试用库。
